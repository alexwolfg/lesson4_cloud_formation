name: "Test"

on:
  workflow_dispatch: 
  push:
    branches:
      - main
jobs:
  composite_test:
    runs-on: ubuntu-latest
    name: Repo composite
    steps:
      - uses: alexwolfg/github_actions_lesson1/.github/workflows/Lesson3_exer1.yaml@main
        with:
          ACTOR: ${{ inputs.actor }}
    
