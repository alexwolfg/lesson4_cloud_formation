name: "Test"

on:
  workflow_dispatch: 
  push:
    branches:
      - main
jobs:
  composite_test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
  
    - name: Use composite action
      uses: alexwolfg/yaml_overview.yaml@main
      with:
        actor: ${{ github.actor }}
    
